" test-setup
" vi: ft=vim

let s:debug = 0

source ./shared-mocks.vimtest

let s:hasArgs = []
function! MOCK_has(arg)
    call add(s:hasArgs, a:arg)
    if a:arg ==# "nvim" 
        return 1
    elseif a:arg ==# "nvim-0.3.2" 
        return 1
    else
        return 0
    endif
endfunction

